
DROP TABLE IF EXISTS todo;
-- CREATE TABLE IF NOT EXISTS todo (
--   id INT IDENTITY NOT NULL PRIMARY KEY,
--   entry VARCHAR(250) NOT NULL,
--   deadline DATE NULL,
--   done BOOLEAN  NULL,
--   priority enum('HIGH','MIDDLE','LOW') NULL);
--
-- INSERT INTO todo (entry, deadline, done, priority) VALUES ('ESSEN', '2010-01-01', false,1);
-- INSERT INTO todo (entry, deadline, done, priority) VALUES ('einkaufen', '2010-01-01', false,1);